FROM mcr.microsoft.com/playwright:v1.56.1-jammy

# Install the CLI package globally (use latest published version)
RUN npm install -g @storybook-visual-regression/cli@latest

# Set entrypoint
ENTRYPOINT ["storybook-visual-regression"]

# Tag this image as the default storybook-visual-regression image
# This allows using it without specifying :dev tag

